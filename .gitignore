# Claude Code internal state (repo root only — leave nested dirs of these names tracked)
/cache/
/debug/
/downloads/
/file-history/
/history.jsonl
/plans/
/plugins/
/projects/
/session-env/
/shell-snapshots/
/specs/
/stats-cache.json
/statsig/
/tasks/
/telemetry/
/todos/

# Browser integration
/chrome/

# Claude Code skill sync — written through the ~/.claude/skills symlink into this repo's skills/
# (`.trash/` is where opting out of the sync moves them)
/skills/synced/
/skills/.trash/

# Machine-specific settings
settings.local.json
.claude.local.md

# Backup files
*.bak

# Python build artifacts (match at any depth)
__pycache__/
.pytest_cache/
