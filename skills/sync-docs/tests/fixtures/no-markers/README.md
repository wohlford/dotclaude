# Backwards-Compat Fixture

This README has no markers. After running `/sync-docs`, this file must be
byte-identical to its original state.

## Section

Some hand-written prose that should never be modified.

| Hand-written | Table |
| :--- | :--- |
| with rows | that aren't in markers |
